<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoTradeTale"
        tools:targetApi="31">
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/Base.Theme.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Base.Theme.AutoTradeTale"
            android:exported="true">
        </activity>

        <!-- Declaration of SignUpActivity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true">
        </activity>
      
        <!-- Declaration of ForgotPasswordActivity -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="true">
        </activity>

        <!-- Declaration of HomeActivity -->
        <activity
            android:name=".HomeActivity"
            android:exported="true">
        </activity>

        <!-- Declaration of FilterActivity -->
        <activity
            android:name=".FilterActivity"
            android:exported="true">
        </activity>

        <!-- Declaration of ProfileActivity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>
